-src //source files
    -chrome
        -content
        -locale
        -skin
    -defaults
        -preference
    chrome.manifest
    install.rdf
    Makefile //GNUmake for src
-bin //binary and build files
    -build //template build files
    -unpackage.xpi binary file

build.sh //bash script for easy build 
Makefile //GNUmake for all
README //it's me
