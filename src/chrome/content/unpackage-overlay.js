/*unpackage.onFirefoxLoad = function(event) {
  document.getElementById("contentAreaContextMenu")
          .addEventListener("popupshowing", function (e){ unpackage.showFirefoxContextMenu(e); }, false);
};

unpackage.showFirefoxContextMenu = function(event) {
  document.getElementById("context-unpackage").hidden = gContextMenu.onImage;
};

window.addEventListener("load", unpackage.onFirefoxLoad, false);*/
